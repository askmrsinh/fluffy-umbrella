#!/usr/bin/env bash

#if [ ! -z "$WWWUSER" ]; then
#    usermod -u $WWWUSER sail
#fi

if [ ! -d /.composer ]; then
    mkdir /.composer
fi

chmod -R ugo+rw /.composer

if [ $# -gt 0 ]; then
    exec "$@"
else
    cp .env.example .env
    composer install -n && php artisan key:generate && php artisan migrate:fresh
    npm install && npm run prod
    /usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf
fi
